# Host-side dependencies for scripts/epaper_dashboard.py (runs outside Docker,
# since it needs direct GPIO/SPI access to the Waveshare e-Paper HAT).
# Install with: pip install -r scripts/requirements-epaper.txt
influxdb-client>=1.36
Pillow>=10.0

# waveshare_epd is not on PyPI - install it from Waveshare's e-Paper repo
# (https://github.com/waveshare/e-Paper) following the driver's own instructions
# for your specific display model.
