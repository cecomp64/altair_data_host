# Host-side dependencies for scripts/charge_cutoff.py (runs outside Docker,
# needs direct GPIO access to drive the charge-cutoff contactor's coil
# driver). Installed automatically by scripts/install-charge-cutoff-deps.sh
# into .venv-charge-cutoff/.
influxdb-client>=1.36

# gpiozero and its lgpio backend (needed on a Raspberry Pi 5's RP1 GPIO chip,
# unlike RPi.GPIO) come from apt (python3-gpiozero, python3-lgpio), not pip -
# the lgpio PyPI package ships no prebuilt wheels and fails to build from
# source without the native liblgpio C library, which apt's python3-lgpio
# already bundles precompiled. The venv is created with
# --system-site-packages so it can see them - see
# scripts/install-charge-cutoff-deps.sh.
